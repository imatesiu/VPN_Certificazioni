# macOS

Installa WireGuard per macOS e importa il profilo reale generato dal container:

```text
wireguard/config/peer_macos1/peer_macos1.conf
```

Il file `sseapid-wireguard.conf` mostra la configurazione attesa, ma non contiene chiavi private reali.

