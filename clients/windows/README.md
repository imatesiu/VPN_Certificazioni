# Windows

Installa WireGuard per Windows e importa il profilo reale generato dal container:

```text
wireguard/config/peer_windows1/peer_windows1.conf
```

Il file `sseapid-wireguard.conf` mostra la configurazione attesa, ma non contiene chiavi private reali.

